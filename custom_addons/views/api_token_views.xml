<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <!-- API Tokens List View -->
        <record id="view_openclaw_api_token_list" model="ir.ui.view">
            <field name="name">openclaw.api.token.list</field>
            <field name="model">openclaw.api.token</field>
            <field name="arch" type="xml">
                <list string="API Tokens" default_order="name">
                    <field name="active" widget="boolean_toggle"/>
                    <field name="name"/>
                    <field name="token" groups="openclaw_gateway.group_openclaw_api_admin"/>
                    <field name="last_used_date"/>
                    <field name="use_count"/>
                    <field name="expiry_date"/>
                    <field name="user_roles" widget="many2many_tags"/>
                </list>
            </field>
        </record>

        <!-- API Tokens Form View -->
        <record id="view_openclaw_api_token_form" model="ir.ui.view">
            <field name="name">openclaw.api.token.form</field>
            <field name="model">openclaw.api.token</field>
            <field name="arch" type="xml">
                <form string="API Token">
                    <sheet>
                        <div class="oe_button_box" name="button_box">
                            <button name="toggle_active" type="object" 
                                    class="oe_stat_button" icon="fa-archive">
                                <field name="active" widget="boolean_button" 
                                       options='{"terminology": "archive"}'/>
                            </button>
                            <button name="action_generate_token" type="object" 
                                    class="oe_stat_button" icon="fa-key"
                                    groups="openclaw_gateway.group_openclaw_api_admin">
                                <div class="o_field_widget o_stat_info">
                                    <span class="o_stat_text">Generate</span>
                                    <span class="o_stat_text">Token</span>
                                </div>
                            </button>
                        </div>
                        <group>
                            <group>
                                <field name="name"/>
                                <field name="token" password="True" 
                                       groups="openclaw_gateway.group_openclaw_api_admin"/>
                                <field name="expiry_date"/>
                            </group>
                            <group>
                                <field name="last_used_date" readonly="1"/>
                                <field name="use_count" readonly="1"/>
                            </group>
                        </group>
                        <group string="Permissions">
                            <field name="user_roles" widget="many2many_tags" nolabel="1"/>
                        </group>
                        <notebook>
                            <page string="Allowed Skills" name="allowed_skills">
                                <field name="allowed_skills" nolabel="1" widget="many2many_tags"/>
                            </page>
                            <page string="IP Access Control" name="ip_control">
                                <group>
                                    <field name="allowed_ip_addresses" nolabel="1" 
                                           placeholder="Leave empty for no IP restrictions&#10;Or enter IPs/ranges separated by commas:&#10;192.168.1.1, 10.0.0.0/24, 172.16.5.10"/>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- API Tokens Action -->
        <record id="action_openclaw_api_token" model="ir.actions.act_window">
            <field name="name">API Tokens</field>
            <field name="res_model">openclaw.api.token</field>
            <field name="view_mode">list,form</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create your first API token!
                </p>
                <p>
                    Tokens provide secure access to the API endpoints.
                    Each token can have specific permissions and IP restrictions.
                </p>
            </field>
        </record>
    </data>
</odoo>